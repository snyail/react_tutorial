# react_tutorial
react tutorial, no more
 
# How to build each stage
## dev
```
docker compose -f docker-compose.dev.yml up -d --build
```

## prod
```
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```
