version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: docker/frontend/Dockerfile
    ports:
      - "80:80"

  ws:
    build:
      context: .
      dockerfile: docker/webserver/Dockerfile
    ports:
      - "3001:3001"
